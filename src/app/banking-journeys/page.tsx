import { Footer } from '@/components/layout/Footer'; import { Navbar } from '@/components/layout/Navbar'; import { PageHero } from '@/components/layout/PageHero'; import { Container } from '@/components/ui/Container'; import { DatabaseContentLibrary } from '@/components/content/DatabaseContent'; import { ContentRepository } from '@/lib/repository';
export const dynamic = 'force-dynamic'; export const revalidate = 0;
export default async function BankingJourneysPage() { const items = await ContentRepository.listByCategory('BANKING_JOURNEY'); return <><Navbar /><main><PageHero eyebrow="Banking Domain Knowledge" title="Explore Banking Journeys" description="Only journeys within your current knowledge access are shown." current="Banking Journeys" /><section className="px-4 py-12 sm:px-6 lg:px-8 lg:py-16"><Container><DatabaseContentLibrary items={items} /></Container></section></main><Footer /></>; }
