import { notFound } from 'next/navigation'; import { reviewRevisionAction } from '@/app/actions'; import { requireRole } from '@/lib/auth'; import { db } from '@/lib/db'; import { canReviewRevision } from '@/lib/permissions'; import { StatusLabel, WorkspaceTitle } from '@/components/workspace/WorkspaceShell';
export default async function ReviewDetail({ params }: { params: Promise<{ revisionId: string }> }) { const user = await requireRole('REVIEWER'), { revisionId } = await params; const revision = await db.contentRevision.findUnique({ where: { id: revisionId }, include: { contentItem: { include: { publishedRevision: true } }, author: { select: { name: true } } } }); if (!revision) notFound(); const allowed = canReviewRevision(user.role, user.id, revision.authorId) && revision.status === 'IN_REVIEW'; return <><WorkspaceTitle eyebrow="Revision Review" title={revision.contentItem.slug.replaceAll('-',' ')} description={`Version ${revision.version} by ${revision.author?.name ?? 'Seed migration'}`} /><StatusLabel status={revision.status} /><div className="mt-6 grid gap-5 xl:grid-cols-2"><Panel title="Submitted revision" content={revision.contentJson} /><Panel title="Current published version" content={revision.contentItem.publishedRevision?.contentJson ?? 'No published version'} /></div>{allowed ? <form action={reviewRevisionAction} className="mt-6 rounded-2xl border border-slate-200 bg-white p-5"><input type="hidden" name="revisionId" value={revision.id} /><label className="font-semibold">Review note<textarea name="reviewNote" rows={4} className="mt-2 w-full rounded-xl border border-slate-300 p-3 font-normal" /></label><div className="mt-4 flex flex-wrap gap-3"><button name="action" value="changes" className="min-h-11 rounded-xl border border-amber-600 px-4 font-semibold text-amber-800">Request Changes</button><button name="action" value="reject" className="min-h-11 rounded-xl border border-red-600 px-4 font-semibold text-red-700">Reject</button><button name="action" value="publish" className="min-h-11 rounded-xl bg-royalBlue px-4 font-semibold text-white">Approve and Publish</button></div></form> : <p className="mt-6 rounded-xl bg-goldPale p-4">This revision is not eligible for review by the current account.</p>}</>; }
function Panel({ title, content }: { title: string; content: string }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">{title}</h2><pre className="mt-3 max-h-[520px] overflow-auto whitespace-pre-wrap text-xs leading-5">{content}</pre></section>; }
