import Link from 'next/link'; import { getAccountAccessState } from '@/lib/membership';
const labels:Record<string,string>={INVITED:'Your account is awaiting activation.',SUSPENDED:'Your account or membership is suspended.',EXPIRED:'Your membership has expired.',PENDING:'Payment or membership approval is pending.',CANCELLED:'Your membership is cancelled.',UNASSIGNED:'No membership is currently assigned.',DISABLED:'This account is unavailable.'};
export default async function Page(){const state=await getAccountAccessState();return <section><p className="text-sm font-semibold text-royalBlue">ACCESS STATUS</p><h1 className="mt-2 text-4xl font-semibold text-navy">Premium access required</h1><div className="mt-7 rounded-2xl border border-amber-200 bg-amber-50 p-6"><p className="font-semibold">{labels[state.kind]??'Your membership is not currently active.'}</p><p className="mt-2 leading-7 text-textSecondary">Premium knowledge is blocked at the server until the account and membership are active.</p></div><div className="mt-6 flex flex-wrap gap-3"><Link href="/account/membership" className="inline-flex min-h-11 items-center rounded-xl bg-navy px-4 font-semibold text-white">View membership</Link><Link href="/account/renewal" className="inline-flex min-h-11 items-center rounded-xl border border-slate-300 px-4 font-semibold">Request renewal</Link><Link href="/" className="inline-flex min-h-11 items-center px-4 font-semibold text-royalBlue">Return home</Link></div></section>}
