import { Footer } from '@/components/layout/Footer'; import { Navbar } from '@/components/layout/Navbar'; import { PageHero } from '@/components/layout/PageHero'; import { Container } from '@/components/ui/Container'; import { DatabaseContentLibrary } from '@/components/content/DatabaseContent'; import { ContentRepository } from '@/lib/repository';
export const dynamic = 'force-dynamic'; export const revalidate = 0;
export default async function CareerRoadmapPage() { const items = await ContentRepository.listByCategory('CAREER_LEVEL'); return <><Navbar /><main><PageHero eyebrow="BA Career Roadmap" title="Build a Clear Path from Foundation to Leadership" description="Only career knowledge within your current access is shown." current="Career Roadmap" /><section className="px-4 py-12 sm:px-6 lg:px-8 lg:py-16"><Container><DatabaseContentLibrary items={items} /></Container></section></main><Footer /></>; }
